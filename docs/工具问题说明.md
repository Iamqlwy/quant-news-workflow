文件 history.py
getpricehistory
返回信息过于简陋，只有起始，结束，最高，最低。
目标：建议直接返回图片。 图片上建议要有均线信息。 
注： 这个工具不要在deepanalysis，使用，要在risk_control使用判断风险

getmarketsnapshot
返回信息过于简陋
目标： 还要有大小盘对比，涨停数量，涨幅前6的板块、概念


get_news_during_period
获取指定区间内相关资讯
这样会找到大量的信息，1天就有几千条
建议：删掉这个该工具

我想增加一个工具，但是没想好

knowledge.py

searchkb，getlessons，getsimilar_cases
建议：限制一下条数，后续可以加一下rerank。


macro.py

get_index_overview
信息过于简陋

update_macro_report
这个是全量修改，不是增量
因为我们的报告是日更的，每天都写一篇新的

market.py

工具本身设计不合理。计算个股技术指标，直接返回数字是很不好的，建议直接返回图片。
这里面大部分工具都要重新设计

review.py

get_analysis 
这个要传入uuid，极为不合理。 agent根本不知道uuid是多少

get_trade同理

getnodehistory 建议只返回 最近5个版本的

writer.py
设计基本合理
但是还是存在传入uuid的情况



我建议先系统性的设计
我有以下思路来解决
1. 把工具的uuid彻底ban掉，模型应该填入名称，而不是id。   然后工具端在实现由名称到id
2. 把uuid信息作为agent状态，程序自动填入，缺点就是会引入大量的uuid，程序也无法确定填入哪一个
3. 你再提供三种思路

